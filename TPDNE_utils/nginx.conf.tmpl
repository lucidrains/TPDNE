server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root {{ root }};

    index {{ index }};

    server_name {{ server_name }};

    location / {
        try_files $uri $uri/ =404;
    }
}
